import { NextRequest, NextResponse } from "next/server";

export default async function requestLoginCode(
  req: NextRequest,
  res: NextResponse
) {
  if (req.method === "post") {
  }

  return {};
}
