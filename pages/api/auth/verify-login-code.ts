import { NextRequest, NextResponse } from "next/server";

export default async function verifyLoginCode(
  req: NextRequest,
  res: NextResponse
) {
  if (req.method === "post") {
  }

  return {};
}
